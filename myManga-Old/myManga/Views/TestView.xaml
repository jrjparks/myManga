<UserControl x:Class="myManga.Views.TestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:bakabox="clr-namespace:System.Windows.Controls;assembly=BakaBox"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ListView
            ItemsSource="{Binding Path=TestItems}"
            Style="{StaticResource ResourceKey=GroupedBalancedWrapListView}">
            <ListView.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <!-- ContainerStyle="{StaticResource ResourceKey=LibraryGroupItem}"-->
                            <TextBlock 
                                Text="{Binding Path=Name}"
                                Foreground="White"
                                FontFamily="Segoe UI"
                                FontSize="18"
                                FontWeight="Bold"/>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </ListView.GroupStyle>
        </ListView>
    </Grid>
</UserControl>

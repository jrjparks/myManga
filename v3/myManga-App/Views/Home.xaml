<UserControl x:Class="myManga_App.Views.Home"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:myManga_App.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="400"
             d:DesignWidth="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" MaxWidth="300px"/>
            <ColumnDefinition Width="15px"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32px"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="32px"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <Button Content="L" FontFamily="Webdings" FontSize="24"/>
                <TextBox Grid.Column="1" BorderBrush="{x:Null}" BorderThickness="0" Background="{x:Null}"></TextBox>
            </Grid>
            <ListBox
                Grid.Row="1" Background="{x:Null}" BorderBrush="{x:Null}" BorderThickness="0">

            </ListBox>
        </Grid>
    </Grid>
</UserControl>

<UserControl
    x:Class="myManga_App.Views.Home"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:vm="clr-namespace:myManga_App.ViewModels"
    mc:Ignorable="d" 
    d:DesignHeight="400"
    d:DesignWidth="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" MaxWidth="300px"/>
            <ColumnDefinition Width="15px"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32px"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>
            <Border 
                BorderBrush="{DynamicResource icon-dark}" 
                BorderThickness="1"
                TextElement.FontSize="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="32px"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Button
                        Tag="{DynamicResource icon-search}"
                        Style="{DynamicResource WithIcon}"/>
                    <TextBox
                        Grid.Column="1" 
                        BorderBrush="{x:Null}" 
                        BorderThickness="0" 
                        Background="{x:Null}"
                        VerticalContentAlignment="Center"/>
                    <Button
                        Grid.Column="2"
                        Padding="2,0"
                        Tag="{DynamicResource icon-remove}"
                        Style="{DynamicResource WithIcon}"/>
                </Grid>
            </Border>
            <ListBox
                Grid.Row="1"
                Background="{x:Null}"
                BorderBrush="{x:Null}"
                BorderThickness="0">
                <ListBox.Items>
                    <ListBoxItem Content="Hello"/>
                    <ListBoxItem Content="World"/>
                    <ListBoxItem Content="123 ABC"/>
                    <ListBoxItem Content="Hello"/>
                    <ListBoxItem Content="World"/>
                    <ListBoxItem Content="123 ABC"/>
                    <ListBoxItem Content="Hello"/>
                    <ListBoxItem Content="World"/>
                    <ListBoxItem Content="123 ABC"/>
                    <ListBoxItem Content="Hello"/>
                    <ListBoxItem Content="World"/>
                    <ListBoxItem Content="123 ABC"/>
                    <ListBoxItem Content="Hello"/>
                    <ListBoxItem Content="World"/>
                    <ListBoxItem Content="123 ABC"/>
                    <ListBoxItem Content="Hello"/>
                    <ListBoxItem Content="World"/>
                    <ListBoxItem Content="123 ABC"/>
                    <ListBoxItem Content="Hello"/>
                    <ListBoxItem Content="World"/>
                    <ListBoxItem Content="123 ABC"/>
                    <ListBoxItem Content="Hello"/>
                    <ListBoxItem Content="World"/>
                    <ListBoxItem Content="123 ABC"/>
                </ListBox.Items>
            </ListBox>
        </Grid>
        <Grid Grid.Column="2">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Top">
                <Button Content="Download" Tag="{DynamicResource icon-cloud-download}" FontSize="20"/>
                <Button Content="Bookmark" Tag="{DynamicResource icon-bookmark}" FontSize="20"/>
                <Button Content="Home" FontSize="20" Tag="{DynamicResource icon-home}"/>
                <ProgressBar Style="{DynamicResource Spinner}" Width="32" Height="Auto"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
